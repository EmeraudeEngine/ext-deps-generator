diff --git a/CMakeLists.txt b/CMakeLists.txt
index 5ab9c52a..f223c040 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -40,9 +40,11 @@ foreach(pold "") # Currently Empty
     endif()
 endforeach()

-# Build the library with C++11 standard support, independent from other including
-# software which may use a different CXX_STANDARD or CMAKE_CXX_STANDARD.
-set(CMAKE_CXX_STANDARD 11)
+# Build the library with C++11 standard support (minimum), but allow override
+# via -DCMAKE_CXX_STANDARD for ABI compatibility with C++17+ consumers.
+if(NOT DEFINED CMAKE_CXX_STANDARD)
+    set(CMAKE_CXX_STANDARD 11)
+endif()
 set(CMAKE_CXX_EXTENSIONS OFF)
 set(CMAKE_CXX_STANDARD_REQUIRED ON)

