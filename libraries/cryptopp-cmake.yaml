name: cryptopp-cmake
source_dir: repositories/cryptopp-cmake
languages: [c, cxx]

cmake_options:
  # Project build policy (AGENTS.md § Build policy): pin the standard, otherwise the build
  # takes the host compiler's default (gnu++17 on gcc 14 today, something else tomorrow) and
  # the archive's API can change with the build machine.
  CMAKE_CXX_STANDARD: 20
  CMAKE_CXX_STANDARD_REQUIRED: true
  CRYPTOPP_USE_INTERMEDIATE_OBJECTS_TARGET: true
  CRYPTOPP_USE_PEM_PACK: true
  CRYPTOPP_BUILD_TESTING: false
  CRYPTOPP_BUILD_DOCUMENTATION: false
  CRYPTOPP_INSTALL: true
  CRYPTOPP_BUILD_SHARED: false
