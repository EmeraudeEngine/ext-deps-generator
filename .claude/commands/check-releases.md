Run `python3 check_releases.py` from the repo root and report the output verbatim.

This is a read-only check — it does not modify any submodule, YAML, or the README.
After reporting, do NOT propose to apply updates unless the user explicitly asks.